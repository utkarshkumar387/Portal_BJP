<% layout('layout') %>
    <% layout('layout') %>
        <div class="row mt-4 home">
            <div class="col-md-3 sidemenu">
                <div class="component">
                    <%- include('../particles/sidebarCommittee.ejs') -%>
                </div>
            </div>
            <div class="col-md-9 home__content mb-5">
                <div class="card">
                    <div class="home__blogsHeader headerMain">
                        <p>All Committees</p>
                    </div>
                    <div class="events">
                        <div class="row">
                            <% for (var i=1; i <=6; i++ ) { %>
                                <div class="col-md-6 committee__card mb-4 px-3">
                                    <%- include('../particles/committeesCard.ejs') -%>
                                </div>
                                <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>